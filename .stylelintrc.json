{
  "extends": ["stylelint-config-standard"],
  "rules": {
    "color-named": "never",
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["theme", "custom-variant", "import", "layer", "plugin"]
      }
    ],
    "import-notation": "string"
  }
}
